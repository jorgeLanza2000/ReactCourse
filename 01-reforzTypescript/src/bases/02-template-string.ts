const firstName = 'Jorge';
const lastName = "Lanza O'Neal";

const fullName = `${firstName} ${lastName} ${1 + 1}`; //!funciones dentro de un string

console.log({ lastName });
console.log(firstName + ' ' + lastName);
console.log(fullName);
